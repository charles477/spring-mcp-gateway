scrape_configs:
  - job_name: spring-mcp-gateway
    metrics_path: /actuator/prometheus
    scrape_interval: 5s
    static_configs:
      # host.docker.internal reaches the gateway running on the host during local dev
      - targets: ["host.docker.internal:8080"]

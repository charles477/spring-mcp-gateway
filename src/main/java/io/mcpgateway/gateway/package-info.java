/**
 * The MCP-spec-compliant JSON-RPC endpoint (tools/list, tools/call) and the request pipeline
 * that orchestrates authn, authz, guardrails, rate limiting, secrets injection, proxying, and audit.
 */
package io.mcpgateway.gateway;
